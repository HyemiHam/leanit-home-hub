# Local development: npm start, then http://localhost:8080/kimhyemi/admin
APP_ORIGIN=http://localhost:8080
ADMIN_USERNAME=admin
# Run npm run admin:password. Keep the hash in SINGLE QUOTES so Compose preserves $.
ADMIN_PASSWORD_HASH=''
DB_PATH=./data/profile.sqlite
PORT=5000
# Production uses NODE_ENV=production and APP_ORIGIN=https://leanit.kr

# Optional existing mail integration
CLIENT_ID=
CLIENT_SECRET=
REFRESH_TOKEN=
VITE_SEND_MAIL_URL=
